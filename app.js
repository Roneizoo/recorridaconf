(function(){
'use strict';
var KEY='recorrida_confinamento_v1';
function defaultPenBlocks(){return [{id:'01',name:'Bloco 01',lines:['A','B','C','D','E','F']},{id:'02',name:'Bloco 02',lines:['G','H','I','J','K']},{id:'03',name:'Bloco 03',lines:['L','M','N','O','P']}];}
function normalizedPenBlocks(saved){var standard=defaultPenBlocks();if(!Array.isArray(saved)||saved.length!==3)return standard;return standard.map(function(block,i){return {id:block.id,name:String((saved[i]||{}).name||block.name),lines:block.lines.slice()};});}
function defaults(){return {version:1,reportDate:'',importedAt:'',hasConsumo:true,lots:[],notes:[],shipmentPlans:[],penBlocks:defaultPenBlocks()};}
function load(){try{var d=JSON.parse(localStorage.getItem(KEY));if(d&&Array.isArray(d.lots)&&Array.isArray(d.notes))return Object.assign(defaults(),d,{penBlocks:normalizedPenBlocks(d.penBlocks)});}catch(e){}return defaults();}
var state=load(),selected=new Set(),selectedKey='';
function save(){localStorage.setItem(KEY,JSON.stringify(state));}
function el(id){return document.getElementById(id);}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function norm(v){return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/\s+/g,' ').trim().toUpperCase();}
function num(v){v=Number(v);return isFinite(v)?v:0;}
function number(v){var x=parseFloat(String(v||'').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,''));return isFinite(x)?x:0;}
function inputNumber(v){var s=String(v==null?'':v).trim();if(!s)return null;s=s.replace(/\s/g,'');if(s.indexOf(',')>=0)s=s.replace(/\./g,'').replace(',','.');var x=parseFloat(s.replace(/[^0-9.-]/g,''));return isFinite(x)?x:null;}
function int(v){return Math.round(Number(v)||0).toLocaleString('pt-BR');}
function decimal(v,d){return Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:d,maximumFractionDigits:d});}
function dateIso(v){var p=String(v).split('/');return p.length===3?p[2]+'-'+p[1].padStart(2,'0')+'-'+p[0].padStart(2,'0'):'';}
function partialDateIso(dd,mm,yy,reportDateIso){dd=dd.padStart(2,'0');mm=mm.padStart(2,'0');var year;if(yy.length>=4){year=yy.slice(0,4);}else if(reportDateIso){var refYear=Number(reportDateIso.slice(0,4)),refMonth=Number(reportDateIso.slice(5,7));year=String(Number(mm)<=refMonth?refYear:refYear-1);}else{year=String(new Date().getFullYear());}return year+'-'+mm+'-'+dd;}
function dateBr(v){if(!v)return '—';var p=v.split('-');return p.length===3?p[2]+'/'+p[1]+'/'+p[0]:v;}
function pad2(n){return String(n).padStart(2,'0');}
function tsFile(d){return d.getFullYear()+'-'+pad2(d.getMonth()+1)+'-'+pad2(d.getDate())+'_'+pad2(d.getHours())+'-'+pad2(d.getMinutes())+'-'+pad2(d.getSeconds());}
function uid(){return Date.now().toString(36)+Math.random().toString(36).slice(2,7);}
function addDaysToIso(iso,days){if(!iso)return '';var d=new Date(iso+'T12:00:00');d.setDate(d.getDate()+days);return d.getFullYear()+'-'+pad2(d.getMonth()+1)+'-'+pad2(d.getDate());}
function lotKey(l){return norm(l.name)+'|'+String(l.entryDate||'');}
function activeNotes(l){var lk=lotKey(l);return state.notes.filter(function(n){return n.status==='active'&&((n.scope==='lot'&&n.targetKey===lk)||(n.scope==='pen'&&n.targetKey===l.pen));});}
function shipmentFor(l){var key=lotKey(l);return (state.shipmentPlans||[]).find(function(p){return p.lotKey===key;})||null;}
function parseShipmentDate(v){var s=String(v||'').trim(),m=s.match(/^(\d{1,2})[\/-](\d{1,2})[\/-](\d{4})$/);if(m)return m[3]+'-'+m[2].padStart(2,'0')+'-'+m[1].padStart(2,'0');m=s.match(/^(\d{4})-(\d{1,2})-(\d{1,2})$/);return m?m[1]+'-'+m[2].padStart(2,'0')+'-'+m[3].padStart(2,'0'):'';}
function showToast(msg){var t=el('toast');t.textContent=msg;t.classList.add('show');clearTimeout(showToast.timer);showToast.timer=setTimeout(function(){t.classList.remove('show');},2800);}
function textRange(row,min,max){return row.filter(function(i){return i.x>=min&&i.x<max;}).map(function(i){return i.text;}).join(' ').trim();}
function nearbyDiet(clean,y,isNew){var lo=isNew?338:340,hi=isNew?400:412;return clean.filter(function(i){var x=i.transform[4],dy=i.transform[5]-y,t=String(i.str||'').trim();return x>=lo&&x<hi&&dy>=-13&&dy<=13&&t&&!/^\d{1,2}\/\d{1,2}\/\d{4}$/.test(t);}).sort(function(a,b){var dd=b.transform[5]-a.transform[5];return Math.abs(dd)>2?dd:a.transform[4]-b.transform[4];}).map(function(i){return String(i.str).trim();}).join(' ').trim();}
function reportColumns(hasConsumo){
  // A Bovino.OS mudou o relatório em set/2026: a coluna "Consumo MN total da baia" foi retirada,
  // o que deslocou (para a direita) todas as colunas seguintes (D. conf., D. trato, GMD, pesos, datas).
  // Por isso as posições em pixel são calibradas em dois conjuntos, escolhidos pela presença dessa coluna.
  return hasConsumo?{entryWeight:[412,441],entryDate:[443,486],days:[486,512],treatmentDays:[512,544],gmd:[544,572],estimatedWeight:[572,604],exitWeight:[604,631],estimatedExit:[632,680],consumption:[680,715],kgCabMS:[715,748],kgCabMN:[748,778],pv:[778,820]}
  :{entryWeight:[400,454],entryDate:[454,493],days:[493,535],treatmentDays:[535,565],gmd:[565,593],estimatedWeight:[593,623],exitWeight:[623,653],estimatedExit:[653,692],consumption:null,kgCabMS:[692,735],kgCabMN:[735,767],pv:[767,830]};
}
function parsePage(items,context){
  var clean=items.filter(function(i){return String(i.str||'').trim();}),groups={},C=reportColumns(context.hasConsumo);
  clean.forEach(function(i){var y=Math.round(i.transform[5]/2)*2;(groups[y]||(groups[y]=[])).push({x:i.transform[4],text:String(i.str).trim()});});
  var all=clean.slice().sort(function(a,b){var d=b.transform[5]-a.transform[5];return Math.abs(d)>2?d:a.transform[4]-b.transform[4];}).map(function(i){return String(i.str).trim();}).join(' '),rm=all.match(/Data\s+de\s+refer[^:]*:\s*(\d{1,2}\/\d{1,2}\/\d{4})/i);
  if(rm){context.reportDate=dateIso(rm[1]);}
  var out=[];Object.keys(groups).map(Number).sort(function(a,b){return b-a;}).forEach(function(y){var row=groups[y].sort(function(a,b){return a.x-b.x;}),joined=row.map(function(i){return i.text;}).join(' '),lm=joined.match(/Linha:\s*(?:LINHA\s*)?(PASTO|[A-P])\b/i);if(lm){context.line=lm[1].toUpperCase();return;}if(context.line==='PASTO'||/Subtotal/i.test(joined))return;
    var name=textRange(row,35,88);if(!/^(LOTE|ENFERMARIA)\b/i.test(name))return;var pen=textRange(row,88,120).replace(/\s+/g,' ').trim(),qty=parseInt(textRange(row,120,152).replace(/\D/g,''),10)||0,line=((pen.match(/^([A-P])\b/i)||[])[1]||context.line||'').toUpperCase();if(!qty||!line)return;
    var entryText=textRange(row,C.entryDate[0],C.entryDate[1]),entryMatch=entryText.match(/(\d{1,2})\/(\d{1,2})\/(\d{2,4})/),exitText=textRange(row,C.estimatedExit[0],C.estimatedExit[1]),exitMatch=exitText.match(/\d{1,2}\/\d{1,2}\/\d{4}/),rawType=textRange(row,298,340),own=/^ANIMAIS?/i.test(norm(rawType));
    var entryDateVal=entryMatch?partialDateIso(entryMatch[1],entryMatch[2],entryMatch[3],context.reportDate):'';
    out.push({name:name,pen:pen,line:line,quantity:qty,deaths:parseInt(textRange(row,152,178).replace(/\D/g,''),10)||0,breed:textRange(row,196,236),category:textRange(row,236,296),rawType:rawType,type:own?'own':'boitel',diet:nearbyDiet(clean,y,!context.hasConsumo)||'NÃO INFORMADA',entryDate:entryDateVal,exitDate:entryDateVal?addDaysToIso(entryDateVal,120):'',days:parseInt(textRange(row,C.days[0],C.days[1]).replace(/\D/g,''),10)||0,treatmentDays:parseInt(textRange(row,C.treatmentDays[0],C.treatmentDays[1]).replace(/\D/g,''),10)||0,entryWeight:number(textRange(row,C.entryWeight[0],C.entryWeight[1])),gmd:number(textRange(row,C.gmd[0],C.gmd[1])),estimatedWeight:number(textRange(row,C.estimatedWeight[0],C.estimatedWeight[1])),exitWeight:number(textRange(row,C.exitWeight[0],C.exitWeight[1])),estimatedExit:exitMatch?dateIso(exitMatch[0]):'',consumption:C.consumption?number(textRange(row,C.consumption[0],C.consumption[1])):0,kgCabMS:number(textRange(row,C.kgCabMS[0],C.kgCabMS[1])),kgCabMN:number(textRange(row,C.kgCabMN[0],C.kgCabMN[1])),pv:number(textRange(row,C.pv[0],C.pv[1]))});
  });return out;
}
async function importPdf(file){
  showToast('Lendo o relatório Bovino.OS...');var pdfjs=window.pdfjsLib;if(!pdfjs)throw new Error('Leitor de PDF indisponível.');pdfjs.GlobalWorkerOptions.workerSrc='pdf.worker.min.js';var doc=await pdfjs.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise,context={line:'',reportDate:'',hasConsumo:true},lots=[];
  for(var n=1;n<=doc.numPages;n++){var page=await doc.getPage(n),tc=await page.getTextContent();if(n===1){var header=tc.items.map(function(i){return String(i.str||'').trim();}).join(' ');if(!/Bovino\.OS/i.test(header)||!/Tipo\s+de\s+entrada/i.test(header))throw new Error('Use o relatório Bovino.OS de Lotes Ativos com a coluna Tipo de entrada.');context.hasConsumo=/consum/i.test(header);}lots=lots.concat(parsePage(tc.items,context));}
  if(!lots.length)throw new Error('Nenhum lote dos currais A a P foi reconhecido.');state.lots=lots;state.reportDate=context.reportDate;state.hasConsumo=!!context.hasConsumo;state.importedAt=new Date().toISOString();save();selectedKey='';renderPens();showToast(lots.length+' lotes e '+int(sum(lots,'quantity'))+' animais atualizados. As anotações foram preservadas.');
}
function sum(rows,key){return rows.reduce(function(s,r){return s+Number(typeof key==='function'?key(r):r[key]||0);},0);}
function kpi(label,value,sub,cls,action){var tag=action?'button':'div';return '<'+tag+' class="kpi '+(cls||'')+(action?' clickable':'')+'"'+(action?' data-kpi="'+action+'" type="button"':'')+'><span>'+label+'</span><strong>'+value+'</strong><small>'+sub+(action?' · toque para filtrar':'')+'</small></'+tag+'>';}
function daysSince(iso){if(!iso)return null;var d=new Date(iso+'T00:00:00'),now=new Date();now.setHours(0,0,0,0);return Math.round((now-d)/86400000);}
function freshnessLabel(iso){var n=daysSince(iso);if(n===null)return '';var cls=n>=3?'stale':n>=1?'aging':'',text=n<=0?'hoje':n===1?'há 1 dia':'há '+n+' dias';return ' <b class="freshness '+cls+'">('+text+')</b>';}
function penNumber(lot){var raw=String(lot.pen||'').toUpperCase(),m=raw.match(/(\d{1,2})/);return m?String(Number(m[1])).padStart(2,'0'):'';}
function lotMatchesPenFilters(l){
  var q=norm(el('searchFilter').value),diet=el('dietFilter').value,note=el('noteFilter').value,min=inputNumber(el('consMin').value),max=inputNumber(el('consMax').value),daysMin=inputNumber(el('daysMin').value),daysMax=inputNumber(el('daysMax').value),shipmentDate=el('shipmentDateFilter').value,shipment=shipmentFor(l);
  var notesCount=activeNotes(l).length,search=!q||norm(l.name+' '+l.pen+' '+l.line+' '+(l.category||'')).indexOf(q)>=0;
  return search&&(!diet||norm(l.diet)===diet)&&(!note||(note==='active'?notesCount>0:notesCount===0))&&(min===null||l.pv>=min)&&(max===null||l.pv<=max)&&(daysMin===null||l.days>=daysMin)&&(daysMax===null||l.days<=daysMax)&&(!shipmentDate||(shipmentDate==='none'?!shipment:!!shipment&&shipment.shipmentDate===shipmentDate));
}
function filtered(){return state.lots.filter(lotMatchesPenFilters);}
function penMapData(applyFilters){
  var map={};state.lots.filter(function(l){return applyFilters===false||lotMatchesPenFilters(l);}).forEach(function(l){var line=String(l.line||'').trim().toUpperCase(),number2=penNumber(l);if(!/^[A-P]$/.test(line)||!number2||Number(number2)>10)return;var key=line+number2;if(!map[key])map[key]={key:key,line:line,number:number2,lots:[],quantity:0,types:{own:0,boitel:0}};map[key].lots.push(l);map[key].quantity+=num(l.quantity);map[key].types[l.type]=(map[key].types[l.type]||0)+num(l.quantity);});return map;
}
function buildAllPens(map){var all=[];state.penBlocks.forEach(function(block){block.lines.forEach(function(line){for(var n=1;n<=10;n++){var key=line+String(n).padStart(2,'0'),pen=map[key]||{key:key,line:line,number:String(n).padStart(2,'0'),lots:[],quantity:0,types:{own:0,boitel:0}};pen.blockId=block.id;all.push(pen);}});});return all;}
function penStatus(pen){if(!pen||!pen.quantity)return 'empty';if(pen.quantity>160)return 'over';if(pen.quantity===160)return 'full';return 'partial';}
function fillPenFilters(){
  var diet=el('dietFilter').value,note=el('noteFilter').value,shipmentDate=el('shipmentDateFilter').value;
  var diets=Array.from(new Set(state.lots.map(function(l){return norm(l.diet);}))).filter(Boolean).sort();
  el('dietFilter').innerHTML='<option value="">Todas</option>'+diets.map(function(d){return '<option value="'+esc(d)+'">'+esc(d)+'</option>';}).join('');
  var shipmentDates=Array.from(new Set((state.shipmentPlans||[]).map(function(p){return p.shipmentDate;}))).filter(Boolean).sort();
  el('shipmentDateFilter').innerHTML='<option value="">Todas</option><option value="none">Sem programação</option>'+shipmentDates.map(function(d){return '<option value="'+d+'">'+dateBr(d)+'</option>';}).join('');
  el('dietFilter').value=diet;el('noteFilter').value=note;el('shipmentDateFilter').value=shipmentDate;
}
function renderPens(){
  fillPenFilters();
  var map=penMapData(),all=buildAllPens(map);
  var allFull=buildAllPens(penMapData(false));
  var term=state.lots.filter(function(l){return norm(l.diet)==='TERMINACAO';}),termLow=term.filter(function(l){return l.pv<1.8;}),activeNotesCount=state.notes.filter(function(n){return n.status==='active';}).length;
  var occupied=allFull.filter(function(p){return p.quantity>0;}),empty=allFull.length-occupied.length,animals=sum(occupied,function(p){return p.quantity;}),internal=sum(occupied,function(p){return Math.max(0,160-p.quantity);});
  el('positionLabel').innerHTML=state.reportDate?'Posição em '+dateBr(state.reportDate)+freshnessLabel(state.reportDate)+' · atualizado '+new Date(state.importedAt).toLocaleString('pt-BR'):'Nenhum relatório importado';
  el('summary').innerHTML=kpi('Capacidade física',int(allFull.length*160),allFull.length+' currais × 160')+kpi('Animais alojados',int(animals),occupied.length+' currais ocupados')+kpi('Capacidade operacional livre',int(empty*160),empty+' currais vazios × 160','gold')+kpi('Espaço perdido',int(internal),'dentro de currais já ocupados','blue')+kpi('Consumo abaixo de 1,8% (terminação)',int(sum(termLow,function(x){return x.quantity;})),'animais em terminação para observar','red','cons-low')+kpi('Anotações ativas',int(activeNotesCount),'acompanhamentos pendentes','blue','notes-active');
  var blockFilter=el('blockFilter'),lineFilter=el('lineFilter'),selectedBlock=blockFilter.value,selectedLine=lineFilter.value,typeFilter=el('typeFilter').value,statusFilter=el('statusFilter').value;
  blockFilter.innerHTML='<option value="">Todos</option>'+state.penBlocks.map(function(b){return '<option value="'+esc(b.id)+'">'+esc(b.name)+'</option>';}).join('');blockFilter.value=selectedBlock;
  lineFilter.innerHTML='<option value="">Todas</option>'+'ABCDEFGHIJKLMNOP'.split('').map(function(l){return '<option value="'+l+'">Linha '+l+'</option>';}).join('');lineFilter.value=selectedLine;
  var contentFilterActive=!!(String(el('searchFilter').value||'').trim()||el('dietFilter').value||el('noteFilter').value||String(el('consMin').value||'').trim()||String(el('consMax').value||'').trim()||String(el('daysMin').value||'').trim()||String(el('daysMax').value||'').trim()||el('shipmentDateFilter').value);
  var blocks=state.penBlocks.filter(function(b){return !selectedBlock||b.id===selectedBlock;}).map(function(block){var lines=block.lines.filter(function(line){return !selectedLine||line===selectedLine;});var lineHtml=lines.map(function(line){var pens=all.filter(function(p){return p.line===line;}).filter(function(p){var status=penStatus(p),contentMatch=!contentFilterActive||p.quantity>0,typeMatch=!typeFilter||(p.types[typeFilter]||0)>0,statusMatch=!statusFilter||(statusFilter==='occupied'?status!=='empty':status===statusFilter);return contentMatch&&typeMatch&&statusMatch;});if(!pens.length)return '';return '<section class="pen-line"><div class="pen-line-head"><strong>Linha '+line+'</strong><span>'+pens.filter(function(p){return p.quantity>0;}).length+' ocupadas</span></div><div class="pen-grid">'+pens.map(renderPenCard).join('')+'</div></section>';}).join('');if(!lineHtml)return '';return '<section class="pen-block"><div class="pen-block-head"><div><span class="section-tag">SETOR '+esc(block.id)+'</span><input data-pen-block-name="'+esc(block.id)+'" value="'+esc(block.name)+'" maxlength="30" aria-label="Nome do bloco '+esc(block.id)+'"></div><span>Linhas '+block.lines[0]+'–'+block.lines[block.lines.length-1]+'</span></div>'+lineHtml+'</section>';}).join('');
  el('penBlocks').innerHTML=blocks||'<div class="empty-state">Nenhum curral corresponde aos filtros escolhidos.</div>';
  renderShipmentSchedule();
  updateSelectionBar();
}
function penCardStatusTitle(status,free){return status==='empty'?'Curral disponível':status==='over'?'Capacidade ultrapassada':status==='full'?'Curral lotado':free+' lugares perdidos';}
function penLotRow(l){
  var notesCount=activeNotes(l).length,key=lotKey(l),checked=selected.has(key),alert=l.pv<1.8||l.pv>2.5,shipment=shipmentFor(l);
  return '<div class="pen-lot-row'+(alert?' alert':'')+(notesCount?' has-note':'')+(shipment?' scheduled':'')+'" data-lot="'+esc(key)+'">'
    +'<label class="pen-lot-select" title="Selecionar para exportar" onclick="event.stopPropagation()"><input type="checkbox" data-select="'+esc(key)+'"'+(checked?' checked':'')+'></label>'
    +'<div class="pen-lot-main"><span class="pen-lot-name">'+esc(l.name)+' · '+int(l.quantity)+' · '+(l.type==='own'?'Próprio':'Boitel')+'</span>'
    +'<span class="pen-lot-meta">'+esc(l.diet||'—')+' · '+decimal(l.pv,2)+'% PV · '+int(l.days)+'d conf.'+(notesCount?' · ● '+notesCount+' anot.':'')+'</span>'+(shipment?'<span class="shipment-badge">Embarque '+dateBr(shipment.shipmentDate)+' · '+int(shipment.quantity)+' animais</span>':'')+'</div></div>';
}
function renderPenCard(p){
  var status=penStatus(p),dominant=p.types.own>=p.types.boitel?'own':'boitel',hasShipment=p.lots.some(function(l){return !!shipmentFor(l);}),cls=status==='empty'?'empty':status==='over'?'over':dominant,free=Math.max(0,160-p.quantity),title=penCardStatusTitle(status,free);
  var lotsHtml=p.lots.map(penLotRow).join('');
  var owners=Array.from(new Set(p.lots.map(function(l){return String(l.category||'').trim();}).filter(Boolean)));
  var ownerHtml=status!=='empty'&&owners.length?'<div class="pen-owner">'+esc(owners.join(' + '))+'</div>':'';
  return '<article class="pen-card '+cls+(hasShipment?' scheduled':'')+'"><div class="pen-card-top"><strong>'+p.key+'</strong><span>'+(hasShipment?'Saída programada':title)+'</span></div>'+ownerHtml+(status==='empty'?'<div class="pen-empty-mark">Livre</div>':'<div class="pen-count"><strong>'+int(p.quantity)+'</strong><span>de 160 animais</span></div><div class="pen-meter"><i style="width:'+Math.min(100,p.quantity/160*100)+'%"></i></div><div class="pen-lots">'+lotsHtml+'</div>')+'</article>';
}
function selectedLots(){var valid={};state.lots.forEach(function(l){valid[lotKey(l)]=l;});Array.from(selected).forEach(function(k){if(!valid[k])selected.delete(k);});return Array.from(selected).map(function(k){return valid[k];}).filter(Boolean);}
function updateSelectionBar(){var lots=selectedLots().sort(function(a,b){return (a.line+a.pen).localeCompare(b.line+b.pen,undefined,{numeric:true});}),bar=el('selectionBar');if(!bar)return;if(!lots.length){bar.hidden=true;return;}bar.hidden=false;el('selectionSummary').textContent=lots.length+' lote'+(lots.length===1?'':'s')+' selecionado'+(lots.length===1?'':'s')+' · '+int(sum(lots,'quantity'))+' animais';el('selectionChips').innerHTML=lots.map(function(l){return '<span class="selection-chip">'+esc(l.pen)+' · '+esc(l.name)+'<button type="button" data-deselect="'+esc(lotKey(l))+'" title="Remover da seleção" aria-label="Remover">×</button></span>';}).join('');}
function currentLot(){return state.lots.find(function(l){return lotKey(l)===selectedKey;});}
function detailItem(label,value){return '<div class="detail-item"><span>'+label+'</span><strong>'+esc(value==null||value===''?'—':value)+'</strong></div>';}
function renderDetail(){var l=currentLot();if(!l){el('drawer').hidden=true;return;}el('detailTitle').textContent='Curral '+l.pen+' · '+l.name;var lk=lotKey(l),shipment=shipmentFor(l),related=state.notes.filter(function(n){return (n.scope==='lot'&&n.targetKey===lk)||(n.scope==='pen'&&n.targetKey===l.pen);}).sort(function(a,b){return b.createdAt.localeCompare(a.createdAt);});el('detailBody').innerHTML=(shipment?'<div class="shipment-detail"><div><strong>Embarque programado para '+dateBr(shipment.shipmentDate)+'</strong><span>'+int(shipment.quantity)+' animais'+(shipment.note?' · '+esc(shipment.note):'')+'</span></div><button type="button" class="shipment-delete" data-delete-shipment="'+esc(lk)+'" title="Excluir esta programação de embarque" aria-label="Excluir programação de embarque">×</button></div>':'')+'<div class="detail-hero"><div class="detail-box"><span>Animais</span><strong>'+int(l.quantity)+'</strong></div><div class="detail-box"><span>Dieta</span><strong>'+esc(l.diet)+'</strong></div><div class="detail-box"><span>Consumo %PV</span><strong>'+decimal(l.pv,2)+'%</strong></div><div class="detail-box"><span>Dias confinamento</span><strong>'+int(l.days)+'</strong></div></div><div class="detail-grid">'+detailItem('Categoria / dono',l.category)+detailItem('Tipo',l.type==='own'?'Próprio':'Boitel')+detailItem('Raça',l.breed)+detailItem('Entrada',dateBr(l.entryDate))+detailItem('Peso entrada',decimal(l.entryWeight,1)+' kg')+detailItem('Peso estimado',decimal(l.estimatedWeight,1)+' kg')+detailItem('Peso de saída',decimal(l.exitWeight,1)+' kg')+detailItem('Saída estimada (relatório)',dateBr(l.estimatedExit))+detailItem('Saída prevista (120 dias)',dateBr(l.exitDate))+detailItem('GMD',decimal(l.gmd,2)+' kg/dia')+detailItem('Consumo MS por cabeça',decimal(l.kgCabMS,2)+' kg/dia')+detailItem('Consumo MS total da baia',decimal(l.kgCabMS*l.quantity,1)+' kg/dia')+detailItem('Consumo MN por cabeça',decimal(l.kgCabMN,2)+' kg/dia')+detailItem('Consumo MN total da baia',state.hasConsumo!==false?decimal(l.consumption,1)+' kg/dia':'não informado neste relatório')+detailItem('Dias de trato',int(l.treatmentDays))+detailItem('Mortes',int(l.deaths))+'</div><div class="notes-layout"><form class="note-form" id="noteForm"><h3>Nova anotação</h3><label>A anotação acompanha<select name="scope"><option value="lot">Este lote, mesmo se mudar de curral</option><option value="pen">Este curral</option></select></label><label>Anotação<textarea name="text" required placeholder="Registre o que foi observado e o que precisa ser acompanhado."></textarea></label><div class="note-actions"><button class="btn primary" type="submit">Salvar anotação</button><button class="btn secondary" type="button" id="closePenNotes">Concluir anotações do curral</button></div></form><section class="notes-panel"><h3>Histórico</h3>'+(related.length?related.map(noteCard).join(''):'<div class="empty-state">Nenhuma anotação registrada.</div>')+'</section></div>';el('noteForm').onsubmit=addNote;el('closePenNotes').onclick=closePenNotes;}
function noteCard(n){return '<article class="note-card '+(n.status==='closed'?'closed':'')+'">'+(n.status==='active'?'<button data-close-note="'+n.id+'">Concluir</button>':'')+'<small>'+(n.scope==='lot'?'LOTE':'CURRAL')+' · '+new Date(n.createdAt).toLocaleString('pt-BR')+(n.closedAt?' · concluída '+new Date(n.closedAt).toLocaleString('pt-BR'):'')+'</small><p>'+esc(n.text)+'</p></article>';}
function addNote(e){e.preventDefault();var l=currentLot(),fd=new FormData(e.target),scope=fd.get('scope'),text=String(fd.get('text')||'').trim();if(!l||!text)return;state.notes.push({id:uid(),scope:scope,targetKey:scope==='lot'?lotKey(l):l.pen,lotName:l.name,pen:l.pen,text:text,status:'active',createdAt:new Date().toISOString(),closedAt:''});save();renderPens();renderDetail();showToast('Anotação salva.');}
function conclude(id){var n=state.notes.find(function(x){return x.id===id;});if(!n)return;n.status='closed';n.closedAt=new Date().toISOString();save();renderPens();renderDetail();showToast('Anotação concluída e mantida no histórico.');}
function closePenNotes(){var l=currentLot(),list=state.notes.filter(function(n){return n.status==='active'&&n.scope==='pen'&&n.targetKey===l.pen;});if(!list.length){showToast('Este curral não possui anotação ativa.');return;}if(!confirm('Concluir as '+list.length+' anotações ativas deste curral? Elas continuarão no histórico.'))return;list.forEach(function(n){n.status='closed';n.closedAt=new Date().toISOString();});save();renderPens();renderDetail();showToast('Anotações do curral concluídas.');}
function clearFilters(){['searchFilter','blockFilter','lineFilter','dietFilter','typeFilter','statusFilter','noteFilter','consMin','consMax','daysMin','daysMax','shipmentDateFilter'].forEach(function(id){el(id).value='';});renderPens();}
function download(name,text,mime){var b=new Blob([text],{type:mime||'application/json'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(u);},500);}
function backup(){download('Recorrida_Confinamento_'+tsFile(new Date())+'.json',JSON.stringify({tipo:'recorrida_confinamento',exportadoEm:new Date().toISOString(),state:state},null,2));showToast('Cópia de segurança salva.');}
function csvField(v){var s=String(v==null?'':v);return /[;"\n]/.test(s)?'"'+s.replace(/"/g,'""')+'"':s;}
function csvSafe(v){var s=String(v==null?'':v);if(/^[=+\-@]/.test(s))s="'"+s;return csvField(s);}
function exportRows(){return selectedLots().sort(function(a,b){return (a.line+a.pen).localeCompare(b.line+b.pen,undefined,{numeric:true});});}
function exportShipmentTemplate(){var lots=exportRows();if(!lots.length){showToast('Selecione no mapa os lotes que serão programados.');return;}
  var title=['Programação de Abate','','','','','','','','','','',''],instruction=['Preenchimento obrigatório: mantenha o lote exatamente como consta no Relatório de Lotes Ativos e informe a data de embarque em DD/MM/AAAA.','','','','','','','','','','',''],head=['Enviar','Linha','Curral','Lote','Data de entrada','Categoria/Dono','Tipo','Animais do lote','Quantidade para abate','Data do embarque (DD/MM/AAAA)','Observação','Identificador'];
  var rows=lots.map(function(l){var p=shipmentFor(l);return ['SIM',l.line,l.pen,l.name,dateBr(l.entryDate),l.category||'',l.type==='own'?'Próprio':'Boitel',l.quantity,p?p.quantity:l.quantity,p?dateBr(p.shipmentDate):'',p?p.note:'',lotKey(l)].map(csvSafe).join(';');});
  var content=[title.map(csvSafe).join(';'),instruction.map(csvSafe).join(';'),'',head.join(';')].concat(rows).join('\r\n');
  download('Programacao_de_Abate_'+tsFile(new Date())+'.csv','﻿'+content,'text/csv');
  showToast('Planilha criada com datas em DD/MM/AAAA. Lote e data de embarque são obrigatórios.');
}
function parseCsv(text){var rows=[],row=[],cell='',quoted=false,s=String(text||'').replace(/^﻿/,'');for(var i=0;i<s.length;i++){var c=s[i];if(quoted){if(c==='"'&&s[i+1]==='"'){cell+='"';i++;}else if(c==='"')quoted=false;else cell+=c;}else if(c==='"')quoted=true;else if(c===';'){row.push(cell);cell='';}else if(c==='\n'){row.push(cell.replace(/\r$/,''));rows.push(row);row=[];cell='';}else cell+=c;}if(cell||row.length){row.push(cell.replace(/\r$/,''));rows.push(row);}return rows;}
async function importShipmentPlan(e){var f=e.target.files&&e.target.files[0];if(!f)return;try{
    var rows=parseCsv(await f.text()),headerAt=rows.findIndex(function(r){return norm(r[0])==='ENVIAR';});if(headerAt<0)throw new Error('Use uma planilha de programação de abate exportada pelo Control Conf ou pela Recorrida.');rows=rows.slice(headerAt);if(rows.length<2)throw new Error('A planilha está vazia.');
    var headers=rows.shift().map(norm),colPart=function(name){return headers.findIndex(function(h){return h.indexOf(norm(name))>=0;});},iSend=colPart('Enviar'),iLot=colPart('Lote'),iQty=colPart('Quantidade para abate'),iDate=colPart('Data do embarque'),iNote=colPart('Observação'),iKey=colPart('Identificador');
    if(iLot<0||iDate<0)throw new Error('Use uma planilha de programação de abate exportada pelo Control Conf ou pela Recorrida.');
    var current={},byName={};state.lots.forEach(function(l){current[lotKey(l)]=l;byName[norm(l.name)]=l;});
    var updated=0,removed=0,errors=[];
    rows.forEach(function(r,n){if(!r.some(function(v){return String(v||'').trim();}))return;var lotName=String(r[iLot]||'').trim(),key=iKey>=0?String(r[iKey]||'').trim():'',lot=current[key]||byName[norm(lotName)],send=norm(iSend>=0?r[iSend]:'SIM');if(!lotName){errors.push('lote obrigatório na linha '+(n+5));return;}if(!lot){errors.push('lote "'+lotName+'" não encontrado');return;}key=lotKey(lot);var existing=(state.shipmentPlans||[]).findIndex(function(p){return p.lotKey===key;});if(send==='NAO'||send==='REMOVER'){if(existing>=0){state.shipmentPlans.splice(existing,1);removed++;}return;}var date=parseShipmentDate(r[iDate]);if(!date){errors.push('data obrigatória para '+lotName+' em DD/MM/AAAA');return;}var quantity=Math.max(1,Math.min(num(lot.quantity),Math.round(inputNumber(iQty>=0?r[iQty]:'')||num(lot.quantity)))),plan={lotKey:key,lotName:lot.name,pen:lot.pen,shipmentDate:date,quantity:quantity,note:String(iNote>=0?r[iNote]||'':'').trim(),importedAt:new Date().toISOString()};if(existing>=0)state.shipmentPlans[existing]=plan;else state.shipmentPlans.push(plan);updated++;});
    save();renderPens();
    if(errors.length)showToast(updated+' programações importadas. Corrija: '+errors.slice(0,3).join('; ')+'.');else showToast(updated+' programações importadas'+(removed?' e '+removed+' removidas':'')+'.');
  }catch(err){showToast(err.message||'Não foi possível importar a programação.');}finally{e.target.value='';}
}
function deleteShipmentPlan(key){if(!key)return;var idx=(state.shipmentPlans||[]).findIndex(function(p){return p.lotKey===key;});if(idx<0)return;if(!confirm('Excluir esta programação de embarque? O lote volta para a situação normal.'))return;state.shipmentPlans.splice(idx,1);save();renderPens();renderDetail();showToast('Programação de embarque excluída.');}
function renderShipmentSchedule(){var current={};state.lots.forEach(function(l){current[lotKey(l)]=l;});var groups={};(state.shipmentPlans||[]).forEach(function(p){var l=current[p.lotKey];if(!l||!p.shipmentDate)return;(groups[p.shipmentDate]||(groups[p.shipmentDate]=[])).push({plan:p,lot:l});});var dates=Object.keys(groups).sort(),box=el('shipmentSchedule');if(!box)return;if(!dates.length){box.innerHTML='';box.hidden=true;return;}box.hidden=false;box.innerHTML='<div class="shipment-title"><strong>Embarques programados</strong><span>Toque em uma data para mostrar os currais.</span></div><div class="shipment-days">'+dates.map(function(d){var rows=groups[d],animals=sum(rows,function(x){return x.plan.quantity;}),pens=Array.from(new Set(rows.map(function(x){return x.lot.pen;}))).join(', ');return '<button type="button" data-shipment-date="'+d+'"><strong>'+dateBr(d)+'</strong><span>'+int(animals)+' animais</span><small>'+esc(pens)+'</small></button>';}).join('')+'</div>';}
function exportPrint(){var lots=exportRows();if(!lots.length){showToast('Selecione ao menos um lote para exportar.');return;}
  var rows=lots.map(function(l){return '<tr><td>'+esc(l.line)+'</td><td>'+esc(l.pen)+'</td><td>'+esc(l.name)+'</td><td>'+esc(l.category||'')+'</td><td>'+(l.type==='own'?'Próprio':'Boitel')+'</td><td>'+esc(l.breed||'')+'</td><td>'+esc(l.diet)+'</td><td>'+int(l.quantity)+'</td><td>'+decimal(l.pv,2)+'%</td><td>'+int(l.days)+'</td><td>'+decimal(l.estimatedWeight,1)+'</td><td>'+dateBr(l.estimatedExit)+'</td></tr>';}).join('');
  el('printArea').innerHTML='<h1>Recorrida do Confinamento — Lotes selecionados</h1><p>Posição em '+dateBr(state.reportDate)+' · gerado em '+new Date().toLocaleString('pt-BR')+' · '+lots.length+' lote'+(lots.length===1?'':'s')+' · '+int(sum(lots,'quantity'))+' animais</p><table><thead><tr><th>Linha</th><th>Curral</th><th>Lote</th><th>Categoria/Dono</th><th>Tipo</th><th>Raça</th><th>Dieta</th><th>Animais</th><th>Consumo %PV</th><th>Dias conf.</th><th>Peso est. (kg)</th><th>Saída estimada</th></tr></thead><tbody>'+rows+'</tbody></table>';
  window.print();
}
function exportCsv(){var lots=exportRows();if(!lots.length){showToast('Selecione ao menos um lote para exportar.');return;}
  var head=['Linha','Curral','Lote','Categoria/Dono','Tipo','Raça','Dieta','Animais','Consumo %PV','Dias confinamento','Peso estimado (kg)','Saída estimada'],rows=lots.map(function(l){return [l.line,l.pen,l.name,l.category||'',l.type==='own'?'Próprio':'Boitel',l.breed||'',l.diet,l.quantity,decimal(l.pv,2),l.days,decimal(l.estimatedWeight,1),dateBr(l.estimatedExit)].map(csvField).join(';');});
  download('Lotes_selecionados_'+tsFile(new Date())+'.csv','﻿'+[head.join(';')].concat(rows).join('\r\n'),'text/csv');
  showToast(lots.length+' lote'+(lots.length===1?'':'s')+' exportado'+(lots.length===1?'':'s')+' em CSV.');
}
function mergeNotes(current,incoming){var map={};current.forEach(function(n){map[n.id]=n;});incoming.forEach(function(n){var ex=map[n.id];if(!ex)map[n.id]=n;else if(n.status==='closed'&&ex.status!=='closed')map[n.id]=n;});return Object.keys(map).map(function(k){return map[k];});}
async function restore(file){
  try{
    var d=JSON.parse(await file.text()),s=d.state||d;
    if(!Array.isArray(s.lots)||!Array.isArray(s.notes))throw new Error();
    var incomingNewer=!!s.importedAt&&(!state.importedAt||s.importedAt>state.importedAt),mergedNotes=mergeNotes(state.notes,s.notes),addedNotes=mergedNotes.length-state.notes.length;
    if(!confirm('Mesclar esta cópia com os dados deste aparelho?\n'+(incomingNewer?'A posição de lotes será atualizada para a mais recente (do arquivo importado).':'A posição de lotes deste aparelho já é a mais recente e será mantida.')+'\nAs anotações dos dois arquivos serão combinadas, sem duplicar e sem apagar nada.'))return;
    state=Object.assign(defaults(),{version:state.version,lots:incomingNewer?s.lots:state.lots,reportDate:incomingNewer?s.reportDate:state.reportDate,importedAt:incomingNewer?s.importedAt:state.importedAt,hasConsumo:incomingNewer?(s.hasConsumo!==false):state.hasConsumo,notes:mergedNotes,shipmentPlans:incomingNewer?(s.shipmentPlans||state.shipmentPlans||[]):(state.shipmentPlans||[]),penBlocks:state.penBlocks});
    save();renderPens();showToast('Cópia mesclada'+(addedNotes>0?': '+addedNotes+' anotação(ões) nova(s) incorporada(s).':'.'));
  }catch(e){showToast('Arquivo de cópia inválido.');}
}
function bind(){
  el('importBtn').onclick=function(){el('pdfInput').click();};el('pdfInput').onchange=async function(){var f=this.files&&this.files[0];if(!f)return;try{await importPdf(f);}catch(e){console.error(e);showToast(e.message||'Não foi possível importar o PDF.');}finally{this.value='';}};
  el('backupBtn').onclick=backup;el('restoreBtn').onclick=function(){el('restoreInput').click();};el('restoreInput').onchange=function(){var f=this.files&&this.files[0];if(f)restore(f);this.value='';};
  ['searchFilter','blockFilter','lineFilter','dietFilter','typeFilter','statusFilter','noteFilter','consMin','consMax','daysMin','daysMax','shipmentDateFilter'].forEach(function(id){el(id).addEventListener(id==='searchFilter'||id.indexOf('cons')===0||id.indexOf('days')===0?'input':'change',renderPens);});
  el('clearFilters').onclick=clearFilters;
  el('importShipmentBtn').onclick=function(){el('shipmentInput').click();};el('shipmentInput').onchange=importShipmentPlan;
  el('exportShipmentBtn').onclick=exportShipmentTemplate;
  el('shipmentSchedule').onclick=function(e){var b=e.target.closest('[data-shipment-date]');if(!b)return;el('shipmentDateFilter').value=b.dataset.shipmentDate;renderPens();};
  document.querySelector('.quick-filters').onclick=function(e){var b=e.target.closest('button');if(!b)return;if(b.dataset.cons==='low'){el('consMin').value='';el('consMax').value='1.8';}if(b.dataset.cons==='high'){el('consMin').value='2.5';el('consMax').value='';}if(b.dataset.diet)el('dietFilter').value=b.dataset.diet;if(b.dataset.notes)el('noteFilter').value=b.dataset.notes;if(b.dataset.days){var r=b.dataset.days.split('-');el('daysMin').value=r[0];el('daysMax').value=r[1];}renderPens();};
  el('summary').onclick=function(e){var b=e.target.closest('[data-kpi]');if(!b)return;if(b.dataset.kpi==='cons-low'){el('consMin').value='';el('consMax').value='1.8';el('dietFilter').value='TERMINACAO';}if(b.dataset.kpi==='notes-active'){el('noteFilter').value='active';}renderPens();};
  el('penBlocks').onclick=function(e){if(e.target.closest('.pen-lot-select'))return;var row=e.target.closest('[data-lot]');if(row){selectedKey=row.dataset.lot;renderDetail();el('drawer').hidden=false;return;}var card=e.target.closest('.pen-card');if(!card)return;var rows=card.querySelectorAll('[data-lot]');if(rows.length===1){selectedKey=rows[0].dataset.lot;renderDetail();el('drawer').hidden=false;}};
  el('penBlocks').onchange=function(e){if(e.target.matches('[data-pen-block-name]')){var block=state.penBlocks.find(function(b){return b.id===e.target.dataset.penBlockName;});if(!block)return;block.name=String(e.target.value||'').trim()||('Bloco '+block.id);e.target.value=block.name;save();renderPens();showToast('Nome do bloco atualizado.');return;}var cb=e.target.closest('[data-select]');if(!cb)return;if(cb.checked)selected.add(cb.dataset.select);else selected.delete(cb.dataset.select);updateSelectionBar();};
  el('selectAllFiltered').onclick=function(){filtered().forEach(function(l){selected.add(lotKey(l));});renderPens();};
  el('clearSelection').onclick=function(){selected.clear();renderPens();};
  el('selectionChips').addEventListener('click',function(e){var b=e.target.closest('[data-deselect]');if(!b)return;selected.delete(b.dataset.deselect);renderPens();});
  el('exportPrintBtn').onclick=exportPrint;el('exportCsvBtn').onclick=exportCsv;
  el('closeDrawer').onclick=function(){el('drawer').hidden=true;};el('drawer').onclick=function(e){if(e.target===this)this.hidden=true;};
  el('detailBody').onclick=function(e){var b=e.target.closest('[data-close-note]');if(b)conclude(b.dataset.closeNote);var d=e.target.closest('[data-delete-shipment]');if(d)deleteShipmentPlan(d.dataset.deleteShipment);};
  document.addEventListener('keydown',function(e){if(e.key==='Escape')el('drawer').hidden=true;});
}
document.addEventListener('DOMContentLoaded',function(){bind();renderPens();if('serviceWorker' in navigator&&location.protocol.indexOf('http')===0)navigator.serviceWorker.register('./sw.js').catch(function(){});});
})();
